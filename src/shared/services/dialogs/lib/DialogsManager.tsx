import GameSettingsDialog from "../../../components/dialogs/GameSettingsDialog";
import GenerateSudokuDialog from "../../../components/dialogs/GenerateSudokuDialog";

const DialogsManager = () => {
    return (
        <>
            <GameSettingsDialog />
            <GenerateSudokuDialog />
        </>
    );
};

export default DialogsManager;
